import React from 'react';
import './Spinner.css'; // We will create this file next

const Spinner = () => (
  <div className="spinner-overlay">
    <div className="spinner"></div>
  </div>
);

export default Spinner;